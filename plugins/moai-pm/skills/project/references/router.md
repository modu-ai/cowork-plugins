# router.md — project 스킬 라우팅 프로토콜 (플러그인 패밀리, 전 코워커)

## 개요

사용자의 자연어 요청을 분석해 project 스킬이 다룰 **AI 코워커(플러그인)** 중 어느 스킬 체인이 중심이 될지 결정하는 프로토콜. 단일 'MoAI-Cowork, 모두의 코워크'(`modu-ai/moai-cowork`) 마켓플레이스의 플러그인 패밀리가 대상이며, 개발 프로젝트 초기화는 이 마켓플레이스의 범위 밖이다. 플러그인 수·스킬 수는 하드코딩하지 않는다 — `.claude-plugin/marketplace.json`이 로스터 정본이다.

---

## 1. 라우팅 흐름

```
사용자 자연어 요청 (/project 진입)
    ↓
[1단계] 프로젝트 컨텍스트 확인 — AGENTS.md "프로젝트 워크플로우"에 이미 정의된 체인 있음 → 직접 실행
    ↓ (없으면)
[2단계] 코워커 키워드 매핑 → 중심 코워커·스킬 체인 결정
    ├── 매칭 1개 코워커 → 해당 코워커 중심 설계
    ├── 매칭 2개+ → §2 모호성 해소 / §3 복합 요청
    └── 매칭 0개 → §4 폴백
```

---

## 2. 키워드 → 코워커 매핑 (요약)

| AI 코워커 (플러그인) | 키워드 |
|--------------------|--------|
| 코워커 (`moai-coworker`) | 사업계획, 창업, 전략, 컨설팅, 제안서, 협상, 프로세스, 주간보고, 목표관리, 여행, 웰니스, 라이프스타일 |
| 작가 (`moai-writer`) | 출판, 원고, 단행본, 전자책, 목차, 퇴고, 출판사 매칭 |
| 스토리 크리에이터 (`moai-story`) | 웹툰, 웹소설, 시나리오, 콘티, 캐릭터 시트, 표지, IP 사업화 |
| 마케터 (`moai-marketer`) | 마케팅, SEO, SNS, 광고, 캠페인, 블로그, 카드뉴스, 뉴스레터, 카피 |
| 미디어 크리에이터 (`moai-media`) | 이미지 생성, 영상 생성, TTS, 더빙, Higgsfield, 프롬프트 빌더 |
| 셀러 (`moai-seller`) | 이커머스, 상세페이지, 스마트스토어, 아임웹, 카페24, 쿠팡, D2C |
| 사무관 (`moai-officer`) | PPT, 한글, hwpx, Word, Excel, PDF, HTML 리포트 |
| 데이터 애널리스트 (`moai-analyst`) | 공공데이터, 시세, 실거래가, KOSIS, DART, 데이터 시각화 |
| 법무 담당 (`moai-lawyer`) | 법률, 계약서, NDA, 컴플라이언스, 법령, 판례, 특허 |
| 재무·세무 담당 (`moai-accountant`) | 세금, 부가세, 결산, 재무제표, IR, 재무모델 |
| 인사·채용 담당 (`moai-recruiter`) | 인사, 채용, 면접, 이력서, 스크리닝, 성과평가 |
| CS매니저 (`moai-cs`) | 고객 문의, CS, 티켓, VOC 분석 |
| 컨설턴트 (`moai-consultant`) | 사업계획서, 비즈니스 모델, TAM/SAM/SOM, 시장 분석 |
| 커리어코치 (`moai-career`) | 이력서 첨삭, 면접 준비, 이직, 구직 |
| 튜터 (`moai-tutor`) | 강의, 교육, 커리큘럼, 시험출제, 논문 |
| 디자이너 (`moai-designer`) | 디자인 시스템, DESIGN.md, Claude Design, 브랜드 |

→ 진입: `cowork-setup.md` (중심 코워커·스킬 체인 설계). 디자이너 중심이면 `designer-setup.md` 서브 프로토콜을 호출한다.

### project 스킬 (PM 허브) 자체 관리 도메인

| 도메인 | 키워드 |
|--------|--------|
| 초기화·라우팅 | 프로젝트 초기화, /project, AGENTS.md 생성 |
| 관리 | /project doctor, /project evolve, /project update (+ 카탈로그·상태·API 키는 자연어) |

---

## 3. 모호성 해소

키워드 매칭 결과 중심 코워커 후보가 2개 이상일 때:

- **자동 해소**: 산출물 유형이 명시되면 해당 산출물을 만드는 코워커 우선. 예: "브랜드 카드뉴스 만들어줘" → 콘텐츠 산출물 명시 → 마케터 중심.
- **사용자 확인**: 자동 해소가 어려우면 `AskUserQuestion`(1질문, 후보 코워커 최대 4개 + Other).

---

## 4. 복합 요청 처리 (크로스 코워커)

요청이 여러 코워커에 걸치면 **하나의 `AGENTS.md` 워크플로우 표에 코워커별 체인을 함께 설계**하고, 반복 작업 유형별 커스텀 에이전트가 체인을 호출하도록 배선한다.

```
"스타트업이라 사업계획서랑 웹사이트 디자인 둘 다 필요해"
→ /project 1회 셋업:
   워크플로우 표 = 코워커(사업계획서 체인) + 디자이너(브랜드 셋업 체인)
   커스텀 에이전트 = biz-plan-writer, brand-system-keeper
```

개발이 섞이면(`"서비스 개발도 해야 해"`) → 개발 환경 셋업은 이 마켓플레이스의 범위 밖임을 안내하고, 비개발 영역(기획·콘텐츠·디자인)만 설계한다.

**심층 분석 판단**: 복합 요청이거나 3개+ 코워커 체인이 걸치면 `ultrathink`(심층 분석)로 최적 설계를 결정한다.

---

## 5. 폴백 전략

| 상황 | 대응 |
|------|------|
| 키워드 매칭 0개 | 기본 코워커 제안 후 확인(대다수 사용자) |
| 중심 코워커 미설치 | Gap Detection → 설치 안내 → "이어서 진행" 재개 |
| 개발 환경 셋업 요청 | 이 마켓플레이스 범위 밖임을 안내하고 비개발 영역만 설계 |

---

## 6. 검증 깊이 연동

라우팅과 함께 **검증 깊이 등급**도 정한다(상세: `execution-protocol.md`):

- 법률·계약·개인정보·세무·재무·정부지원·의료·규제 키워드, 또는 2개+ 코워커 체인 → **DEEP**
- 일반 산출물(보고서·카피·문서) → **NORMAL**
- 단순 조회·요약 → **QUICK**

키워드는 seed일 뿐이며, 트리거 단어 없이 표현된 고위험 요청도 맥락으로 DEEP 판단한다.
